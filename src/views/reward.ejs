<%- include("girl", {girls: reward.girls}) -%>
<% if ( reward.softCurrency ) { %>
    +<%= reward.softCurrency %>$<br />
<% } %>
<% if ( reward.hardCurrency ) { %>
    +<%= reward.hardCurrency %> koban<br />
<% } %>
<% if ( reward.leaguePoints ) { %>
    +<%= reward.leaguePoints %> points de league<br />
<% } %>
<% if ( reward.victoryPoints ) { %>
    +<%= reward.victoryPoints %> mojo<br />
<% } %>
<% if ( reward.xp ) { %>
    +<%= reward.xp %> XP<br />
<% } %>
<%- include("girl-shards", {source: reward}) -%>
<%- include("item", {source: reward}) -%>
