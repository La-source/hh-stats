<h2><%= __n("event_contest", event.contest.nbContest) %></h2>
<% if ( event.contest.softCurrency !== 0 ) { %>
    +<%= formatNumber(event.contest.softCurrency) %><%= __("unit_softCurrency") %><br />
<% } %>
<% if ( event.contest.hardCurrency !== 0 ) { %>
    +<%= formatNumber(event.contest.hardCurrency) %> <%= __("unit_hardCurrency") %><br />
<% } %>
